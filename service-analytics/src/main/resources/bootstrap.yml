spring:
  application:
    name: service-analytics
  cloud:
    config:
      enabled: true
      uri: http://config:8888
      fail-fast: true
      password: ${CONFIG_SERVICE_PASSWORD}
      username: user

